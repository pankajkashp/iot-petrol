{
  "name": "@fuel/analytics-engine",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"analytics-engine lint placeholder\"",
    "clean": "rm -rf dist"
  }
}
