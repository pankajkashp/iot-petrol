{
  "name": "@fuel/billing-engine",
  "private": true,
  "version": "0.1.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"billing-engine lint placeholder\"",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@fuel/shared-types": "*"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
